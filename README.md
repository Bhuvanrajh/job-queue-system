# job-queue-system
A secure distributed job queue system built using TCP socket programming with SSL/TLS. Supports multiple clients and workers, dynamic job distribution, and fault tolerance through job re-queuing.
